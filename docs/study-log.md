# Registro de estudo

## 2026-07-15 - Setup inicial

Tema: ambiente full-stack do laboratorio.

Conceito praticado: separacao entre backend API e frontend SPA em um monorepo com workspaces.

Exercicio realizado: preparar API AdonisJS 5 com `GET /health` e tela Vue que consulta esse endpoint.

Dificuldade encontrada: validar compatibilidade de dependencias antigas do AdonisJS 5 com o Node local.

Proximo ponto recomendado: explicar o fluxo `rota -> middleware -> handler -> resposta` do endpoint `GET /health`.
